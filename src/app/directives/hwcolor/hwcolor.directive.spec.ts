import { HwcolorDirective } from './hwcolor.directive';

describe('HwcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new HwcolorDirective();
    expect(directive).toBeTruthy();
  });
});
